# URL base del backend Express.
# Se NON la imposti si usa comunque il backend di produzione: questo file
# serve per puntare altrove, cioè a un backend locale.
#   backend locale (nodemon)       http://localhost:3001
#   backend usa e getta della skill http://localhost:3011
VITE_API_URL=https://detoma-backend.vercel.app
